# =============================================================================
# REQUIRED SETTINGS - You MUST change these before starting the application
# =============================================================================

# Password for web interface (admin)
# SECURITY: Change this to a strong, unique password!
APP_PASSWORD=

# JWT secret key for authentication tokens
# SECURITY: Generate a random 32+ character string (e.g., openssl rand -hex 32)
SECRET_KEY=

# =============================================================================
# OPTIONAL SETTINGS - These have sensible defaults
# =============================================================================

# Public URL for RSS feed links
# This should be the URL where your public port (8080) is accessible
# e.g., https://podcasts.example.com or http://your-server-ip:8080
BASE_URL=http://localhost:8080

# Admin origin for CORS (where the admin UI is served from)
# This should match the URL you use to access the admin interface
# e.g., http://localhost:3000 or http://your-server-ip:3000
ADMIN_ORIGIN=http://localhost:3000

# =============================================================================
# PORT CONFIGURATION (in docker-compose.yml)
# =============================================================================
# - Public port 8080: RSS feeds, audio files, artwork (safe to expose to internet)
# - Admin port 3000: Web UI and API (local network only - do NOT port forward to internet)
